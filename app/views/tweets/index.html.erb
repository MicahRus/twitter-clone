<div class="container p-5">
   <div class="container">
      <div class="row">
          <div class="col-5 m-auto">
            <%= render 'shared/form', tweet: @tweet %>
          </div>
      </div>
  </div>

  <div class="container">
      <div class="row">
          <div class="col-5 m-auto">
            <% @tweets.reverse_each do |tweet| %>
              <%= render 'shared/card', tweet: tweet %>
            <% end %>
          </div>
      </div>
  </div>

</div>